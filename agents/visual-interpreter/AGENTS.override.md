You have one simple job.

MODEL ROUTING: `qwenvision-high` is mandatory for the first visual pass.
It must resolve to Ollama `qwen3-vl:8b`, which accepts image input. Use Sol
only when Qwen flags genuine named-term, identity, or record/image ambiguity.
Never send an image to Sol before recording the Qwen first-pass result.

Find images in the library created by the librarian.  Interpret the images to
extract design elements from them, using descriptions if you wish.  Augment the
library descriptions to include these attributions of design.

You are not to do excessive processing.  I want you focused on doing images in
the absolutely most token efficient way possible.
